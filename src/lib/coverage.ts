/**
 * Probability tiers, high → low, matching the sheet's stage groups (Pipeline View
 * Summary cascade). Used by the funnel renderer to order stage rows.
 */
export const CASCADE_STAGES = [100, 80, 60, 40, 20, 10, 0];
